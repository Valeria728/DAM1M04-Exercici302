{{> header}}
{{> menu}}

<h1>👥 Customers</h1>

{{#each customers}}
  <div>
    <h3>{{first_name}} {{last_name}}</h3>
    <strong>Rentals:</strong>
    <ul>
      {{#each rentals}}
        <li>{{rental_date}}</li>
      {{/each}}
    </ul>
  </div>
{{/each}}

{{> footer}}